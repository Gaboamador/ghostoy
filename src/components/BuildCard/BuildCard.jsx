import{Link}from'react-router-dom';import{motion}from'framer-motion';import{HiOutlineArrowRight}from'react-icons/hi';import{useBuildProgress}from'../../hooks/useBuildProgress';import ProgressBar from'../ProgressBar';import styles from'./BuildCard.module.scss';export default function BuildCard({build}){const p=useBuildProgress(build);return <motion.article className={styles.card} initial={{opacity:0,y:8}} animate={{opacity:1,y:0}}><div><span className="badge">{build.category}</span><h2>{build.armor.name}</h2><small className="muted">{build.armor.perkFocus}</small></div><div className={styles.progress}><span>{p.obtained}/{p.total} elementos</span><strong>{p.percent}%</strong></div><ProgressBar value={p.percent}/><p className={styles.missing}>{p.total-p.obtained} pendientes</p><Link className={styles.link} to={`/builds/${build.id}`}>Ver build <HiOutlineArrowRight/></Link></motion.article>}
