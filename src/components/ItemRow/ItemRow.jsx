import{HiCheck,HiOutlineLocationMarker}from'react-icons/hi';import styles from'./ItemRow.module.scss';export default function ItemRow({item,checked,onToggle,subtitle}){const description=item.effect||item.perkFocus;return <article className={`${styles.row} ${checked?styles.done:''}`}><button onClick={onToggle} className={styles.check} aria-label={`Marcar ${item.name}`}>{checked&&<HiCheck/>}</button><div><h3>{item.name}</h3>{subtitle&&<span className="badge">{subtitle}</span>}<p>{description||'Efecto no disponible en la fuente provista.'}</p>{item.effects?.length>0&&<ul>{item.effects.map(effect=><li key={effect}>{effect}</li>)}</ul>}<small><HiOutlineLocationMarker/>{item.howToGet||'Cómo obtenerlo: información no disponible.'}</small>{item.upgradeMethod&&<p className={styles.upgrade}><strong>Mejora:</strong> {item.upgradeMethod}</p>}</div></article>}
